#pragma once/*void squareStep(Matrix<int>Array, int x, int y, int reach, unsigned int Width);

void diamondStep(Matrix<int>Array, int x, int y, int reach, unsigned int Width);

void diamondSquare(Matrix<int> Array, int size, unsigned int Width);
*/
